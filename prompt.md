请实现“AI 出题”相关逻辑：每次 AI 出题时生成一个 id 并插入一个队列中，队列中记录 id、题目类型、题目创建时间、UID、状态（正在生成、生成完毕、已作答）、题面（如果已经生成完毕）、上一次的作答情况、生成时使用的单词列表（用单词 id 存储）；队列使用 Postgresql + Prisma 存储。

同时你需要实现一个后端接口，不断读取队头，并调用 AI 接口进行题目生成，将生成后的题面插入队列中。生成题目的 Prompt 需要我自行编写。