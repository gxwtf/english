# PDF Export Fonts

The compact wordbook PDF export loads these deployment assets at runtime:

- `simsun.ttf`: SimSun / 宋体
- `times.ttf`: Times New Roman regular
- `timesbd.ttf`: Times New Roman bold

These font binaries are intentionally ignored by git. Deploy them to this directory on each runtime environment before using the vector wordbook PDF export.
