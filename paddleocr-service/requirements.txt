# PaddleOCR 服务依赖
# 安装：./setup.sh（自动创建 venv 并安装）
# 版本约束：paddlepaddle 锁 2.x（避免 3.0 破坏性变更），numpy 锁 <2.0（paddleocr 兼容性）
paddlepaddle>=2.6,<3.0
paddleocr>=2.7
opencv-python>=4.5
flask>=3.0
numpy>=1.20,<2.0
